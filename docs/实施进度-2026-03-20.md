[任务] core 子模块仓库来源切换
[时间] 2026-03-20

[目标]
- 将 LSPatch 的 `core` 子模块来源从 `JingMatrix/LSPosed` 切换为 `roro2239/LSPosed`。

[实施]
1. 修改 `.gitmodules` 中 `submodule "core".url`。
2. 在 GitHub 仓库直接提交变更到 `master`。

[结果]
- `.gitmodules` 已更新为 `https://github.com/roro2239/LSPosed.git`。

[备注]
- 本次仅调整子模块来源，不改动 `core` 目录内业务代码和文案链接。